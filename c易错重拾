变量定义当前代码块之前；
const可解释为只读，修饰的不可改变
static 修饰静态变量，静态局部变量生命周期延长即在同一原文件内，函数作用域{}外可改变；
字符串/0结束标志符，不计字符串长度，EOF文件结束标识符值=-1；
转义标识符多加一个\才不会被转义；
负数内存存储是二进制补码，打印是原码，正数三码一样，负数补码=反码+1；
后置++，先代入在++，前置++，先++再代入；
宏定义 #define MAX(X,Y) (X>Y? X:Y)
typedef 重新给变量起别名；
~按位取反；
int*  指针变量为int，指向地址；
*p 是解引用，指向地址的值；
结构体变量的成员如果是数组，不可以.来更改，要复制strcpy（）把要改的内容覆盖过去；
指针p->成员，即地址指向值操作，等同.操作；
